---
import type { CollectionEntry } from 'astro:content';

interface Props {
  publication: CollectionEntry<'publications'>;
}

const { publication } = Astro.props;
const { data } = publication;
const href = data.type === 'external'
  ? (data.externalUrl ?? '#')
  : `/writing/${publication.id}/`;
const isExternal = data.type === 'external';
const formattedDate = data.datePublished.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric'
});
---

<article class="publication-card">
  <div class="publication-meta">
    <span>{data.area}</span>
    <span aria-hidden="true">·</span>
    <span>{data.venue ?? (isExternal ? 'Published elsewhere' : 'Sergey Kyunttsel')}</span>
    <span aria-hidden="true">·</span>
    <time datetime={data.datePublished.toISOString()}>{formattedDate}</time>
  </div>

  <h2><a href={href} target={isExternal ? '_blank' : undefined} rel={isExternal ? 'noopener noreferrer' : undefined}>{data.title}</a></h2>
  <p>{data.deck}</p>

  <a class="publication-link" href={href} target={isExternal ? '_blank' : undefined} rel={isExternal ? 'noopener noreferrer' : undefined}>
    {isExternal ? 'Read at original source ↗' : 'Read article →'}
  </a>
</article>
